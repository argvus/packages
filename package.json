{
  "name": "argvus-packages",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "packages:prune": "node scripts/prune-arch-packages.mjs",
    "packages:prune:apply": "node scripts/prune-arch-packages.mjs --apply"
  },
  "dependencies": {
    "astro": "7.2.0"
  },
  "devDependencies": {
    "@types/node": "24.2.0"
  }
}
